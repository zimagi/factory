

#
# Define data related command mixin with autogenerated field and method accessors
#
command_mixins:
    pull_request_comment:
        aliases: ["pull_request_comment_optional"]
        class: PullRequestCommentMixin
        mixins: ['comment']
        meta:
            pull_request_comment:
                data: pull_request_comment
                relations: true

    pull_request_comments:
        class: PullRequestCommentMultipleMixin
        mixins: ['comment']
        meta:
            pull_request_comments:
                data: pull_request_comment
                relations: true

#
# Define data related base command
#
command_base:
    pull_request_comment:
        mixins: ["pull_request_comment"]
        groups_allowed: ['dev-team']

#
# Define data resource commands (list, get, save, remove, clear)
#
command:
    pull_request_comment:
        resource: pull_request_comment
        base: pull_request_comment
        base_name: pull_request_comment
        priority: 100